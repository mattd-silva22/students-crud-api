export enum EErrors {
  INVALID_CREDENTIALS = "Invalid credentials",
  INVALID_TOKEN = "Invalid token",
  BAD_REQUEST = "Bad request",
  INTERNAL_SERVER_ERROR = "Internal server error",
  DATABASE_ERROR = "Database error",
  DATABASE_FAIL = "Database fail",
}
